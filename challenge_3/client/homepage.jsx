const HomePage = ({ handleClick }) => (
  <div>
    <button id="SignUpPage" onClick={handleClick}>
      Sign Up
    </button >
    <button id="LogInPage" onClick={handleClick}>
      Log In
    </button >
  </div>
)
